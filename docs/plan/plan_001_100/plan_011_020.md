# Plan Phase 011-020 Summary

This file is a compact planning index for agents. Keep `docs/plan/plan.md` as the source of truth.

## phase_011

Phase Goal
- Project-local skill descriptions should stay concise so the skills index loads without context-budget warnings.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_012

Phase Goal
- The product layout follows a ChatGPT-like left control rail with a central conversation workspace.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_013

Phase Goal
- Team login, user boundaries, and deployment exploration.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_014

Phase Goal
- Conversation follow-up handling can reuse the last resolved stock while still requiring an explicit investment horizon.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_015

Phase Goal
- Local runner startup may open the frontend browser automatically, but the behavior must remain disableable for validation and automation.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_016

Phase Goal
- Developer setup can write provider credentials through the same backend service contract used by future UI settings.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_017

Phase Goal
- Frontend credential UI must use masked credential status and send raw keys only on explicit save.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_018

Phase Goal
- Raw BYOK credentials are decrypted only at the live provider-call edge and must not appear in prompts, responses, logs, or stored analysis records.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_019

Phase Goal
- Local `.env` keys may configure search/news/market-data providers, but LLM analysis no longer uses environment API-key fallback after `phase_035`.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_020

Phase Goal
- Concurrent conversation appends merge against the latest stored messages before writing local JSON state.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.
