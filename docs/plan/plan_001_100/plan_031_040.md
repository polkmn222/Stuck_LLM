# Plan Phase 031-040 Summary

This file is a compact planning index for agents. Keep `docs/plan/plan.md` as the source of truth.

## phase_031

Phase Goal
- Add deterministic offline evals before expanding source safety and source quality so cutoff, grounding, probability, and confidence invariants can be regression-tested without live providers.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_032

Phase Goal
- Treat source safety as deterministic eval coverage first, before changing live prompts or source-quality weighting.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_033

Phase Goal
- Keep source quality eval-only until the product UI and scoring semantics are ready to expose quality fields.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_034

Phase Goal
- Tighten the existing live-analysis prompt contract instead of adding a parallel analysis package or broad prompt rewrite.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_035

Phase Goal
- Environment variables are not used as LLM credential fallback; missing saved keys return setup-needed copy and the chat UI renders the API-key prompt in error red.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_036

Phase Goal
- Minimal reusable market quote card in chat and analysis with stock name, symbol, price, exchange, as-of timestamp, and SVG line chart.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_037

Phase Goal
- Cerebras provider header compatibility with explicit JSON accept and non-urllib User-Agent, verified through the saved credential connection test.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_038

Phase Goal
- LLM provider credentials remain saved-user-key only, while `SERPAPI_API_KEY` and other search/news/market-data keys stay environment-configured and separate.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_039

Phase Goal
- Market snapshots belong to the message that produced them so later follow-up messages cannot erase earlier charts, news, or stats from the chat history.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_040

Phase Goal
- Provider/API-key work is incomplete unless the saved key is exercised through `/conversations` and repeated same-conversation follow-up, not only Settings diagnostics.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.
