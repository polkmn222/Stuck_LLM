# Plan Phase 071-080 Summary

This file is a compact planning index for agents. Keep `docs/plan/plan.md` as the source of truth.

## phase_071

Phase Goal
- `run-all.sh` Ctrl+C shutdown handling so backend and frontend terminate together.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_072

Phase Goal
- News query and ranking refinement to demote quote/history pages and target real company news.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_073

Phase Goal
- News importance categories and priority ordering across official, earnings, business, controversy, and market stories.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_074

Phase Goal
- LLM JSON news summary parsing for Korean article headlines and summaries.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_075

Phase Goal
- ChatGPT-style Korean news digest UI with key headlines, favicon cards, and overflow-safe snippets.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_076

Phase Goal
- News polish documentation, backups, and full validation.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_077

Phase Goal
- Prediction-like no-horizon requests default to `swing`, presented as the next five trading days; generic analysis requests may still ask for horizon.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_078

Phase Goal
- Current market snapshot and eligible chart context may become `market_data` evidence at `as_of_at`; future PnL/backtest data remains excluded from analysis prompts.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_079

Phase Goal
- Expected return range is a rough five-trading-day heuristic derived from directional score edge and confidence until calibrated backtests exist.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_080

Phase Goal
- Buy-date questions return separate PnL simulations and must not create analysis requests or feed future prices into historical LLM evidence.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.
