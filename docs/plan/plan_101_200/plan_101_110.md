# Plan Phase 101-110 Summary

This file is a compact planning index for agents. Keep `docs/plan/plan.md` as the source of truth.

## phase_101

Phase Goal
- Centralized environment-backed external provider credential boundary for search/news/market-data keys.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_102

Phase Goal
- Backend E2E chat-to-prediction-analysis test slice for `/conversations` persistence and scoring.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_103

Phase Goal
- Chat news digest component extraction from `ChatShell` with standalone frontend coverage.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_104

Phase Goal
- News digest prompt and LLM-output formatting extraction from `conversations/service.py`.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_105

Phase Goal
- Local state sidecar write optimization for unchanged cache/artifact domains.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_106

Phase Goal
- News digest UI labels moved into the shared i18n copy boundary.

Completed Work
- Reflected in the planning log for this phase range.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Architecture decisions and policy notes remain in `docs/plan/plan.md`.

Next Steps
- Read the latest range summary before opening the full planning log.

## phase_107

Phase Goal
- Compact planning, task, and implementation documentation into 100-phase folders and 10-phase summary files.

Completed Work
- Added range README indexes and standardized summary file naming for the current `phase_101` through `phase_110` bucket.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Original detailed plan, task, and implementation logs remain the source of truth.

Next Steps
- Continue future documentation summaries in full 10-phase ranges.

## phase_108 - LLM Agent Product Spec Documentation

Phase Goal
- Add a durable product behavior contract for the user-facing LLM stock-analysis agent.

Completed Work
- Defined `docs/product/` as the home for product-facing LLM agent behavior, UI rules, runtime flow, evidence boundaries, response rules, provider rules, cache rules, and maintenance policy.
- Linked product docs from repository entry points and workflow docs so future implementation agents can find the behavior contract before changing related code.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- Product specs are separate from phase logs and agent workflow instructions.
- Future feature or behavior changes that affect product specs should ask the user before updating `docs/product/`, unless the current request already includes that documentation update.

Next Steps
- Read `docs/product/README.md` before changing user-facing LLM agent behavior, workspace UI rules, evidence rules, response shape, provider behavior, cache semantics, or runtime flow.

## phase_109 - Product Spec Detail And Roadmap

Phase Goal
- Clarify whether the product spec includes news discovery and prediction logic, then make the next implementation phases explicit.

Completed Work
- Expanded `docs/product/llm-agent-spec.md` with explicit news discovery, prediction, analysis-boundary, cache, and PnL separation rules.
- Added `docs/product/llm-agent-phase-roadmap.md` for phases `phase_109` through `phase_118`.
- Linked the roadmap from the product docs index and repository README.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- The product spec now includes news discovery and prediction logic in explicit product-contract form.
- Later phases should implement those contracts in focused backend/frontend slices with tests.

Next Steps
- Start implementation at `phase_110 - News Retrieval Pipeline` unless the user reprioritizes.

## phase_110 - LLM Agent Contract Tests And News Category Boundary

Phase Goal
- Convert the product roadmap into executable backend contract coverage across news, prediction, scoring, cache, and PnL boundaries.

Completed Work
- Added contract tests for news query planning, canonical URL dedupe, category ranking, diversity, prediction `as_of_at` exclusion, cache reuse/miss boundaries, probability normalization, confidence penalty, and PnL separation.
- Adjusted news article category priority so product launch/device news is classified as `product_service` before broader `core_business`.

Changed Files
- See the matching `docs/task` and `docs/implement` summaries for file-level detail.

Important Notes
- This phase adds backend contract coverage for the product roadmap without broad service extraction.

Next Steps
- Continue with `phase_111 - Evidence Normalization` as the next focused runtime slice.
