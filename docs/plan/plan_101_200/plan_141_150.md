# Plan Phase 141-150 Summary

This file is a compact planning index for agents. Keep the detailed source log separate when it exists.

## phase_141 - Claude Review Documentation Integrity

Phase Goal
- Restore documentation index integrity and encode the review's recurring workflow rules before code cleanup phases continue.

Completed Work
- Add missing phase 138-140 index entries to the range READMEs.
- Create the 141-150 compact phase bucket and route future work to it.
- Add EventRegistry to the local environment template.
- Clarify provider cache-version and live market-data test-isolation workflow rules.

Next Steps
- Keep subsequent review cleanup phases small enough to validate independently.

## phase_142 - Market Data Test Isolation

Phase Goal
- Prevent backend tests with fixed market-data expectations from depending on live `FinanceDataReader` results.

Completed Work
- Add a deterministic test fixture that blocks unmocked live FinanceDataReader reads.
- Preserve focused provider tests that intentionally monkeypatch market-data adapters.

Next Steps
- Run the prior drift tests and the full backend suite after the fixture is in place.

## phase_143 - News Provider Robustness Cleanup

Phase Goal
- Clean up remaining news provider review findings without changing the product contract.

Completed Work
- Pin EventRegistry calls to the `www.eventregistry.org` host.
- Replace the time-bound social query suffix with neutral query context.
- Add partial provider-error status support and Reddit 429 early exit.
- Bump the news provider cache version after provider behavior changes.

Next Steps
- Keep provider tests fake-backed and avoid live network calls.

## phase_144 - News Evidence Timestamp Clamp

Phase Goal
- Stop conversation evidence conversion from synthesizing source documents published after quote `as_of_at`.

Completed Work
- Clamp news digest source-document `published_at` fallback values to the quote cutoff when fallback timestamps are later than the quote.
- Add focused coverage for future-dated digest generated time and missing article date.

Next Steps
- Keep downstream analysis exclusion rules intact; this phase only prevents avoidable future-dated source metadata.

## phase_145 - News Digest Card Privacy Cleanup

Phase Goal
- Remove duplicate news card title rendering and avoid leaking article URLs to Google favicon infrastructure.

Completed Work
- Make the visible headline the single link target.
- Replace remote favicon requests with the existing local fallback glyph.

Next Steps
- Validate focused frontend tests and typecheck.

## phase_146 - Settings Provider Type Cleanup

Phase Goal
- Remove stale settings provider literals that no longer represent actual credential provider selection.

Completed Work
- Drop or neutralize dead `AppSettings.provider` usage in frontend and backend settings schemas.
- Update API mapping, defaults, and tests so active credential profiles remain the source of truth.

Next Steps
- Run frontend API/settings tests and backend settings tests after the type cleanup.

## phase_147 - Crawl URL Safety Boundary

Phase Goal
- Close user-supplied URL crawl gaps from review without changing the news digest product contract.

Completed Work
- Resolve crawl hostnames before `urlopen` and fail closed on private, loopback, link-local, reserved, multicast, or unspecified addresses.
- Apply the same resolved-host policy to redirect targets before reading response bodies.
- Reject unsupported crawl response content types while preserving missing `Content-Type` compatibility.

Next Steps
- Keep direct URL crawl tests fake-backed and avoid live network dependencies.

## phase_148 - Settings Credential Profile Actions

Phase Goal
- Make saved credential profile actions reachable from Settings.

Completed Work
- Save newly entered LLM keys as new profiles instead of overwriting the active profile.
- Render delete actions for every saved external news/search key.

Next Steps
- Keep UI tests focused on profile creation, selection, deletion, and raw-secret non-exposure.

## phase_149 - Analysis External Credential Source Collection

Phase Goal
- Thread selected external news/search credentials through stock-analysis source collection.

Completed Work
- Allow ingestion collectors for Tavily, GNews, and SerpApi to use the selected external credential map before falling back to environment credentials.
- Pass active external credentials from conversation analysis orchestration into `collect_sources`.

Next Steps
- Keep news digest and stock-analysis credential boundaries aligned when adding new external providers.
