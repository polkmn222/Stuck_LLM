# Task Phase 141-150 Summary

This file is a compact task index for agents. Keep the detailed source log separate when it exists.

## phase_141 - Claude Review Documentation Integrity

Phase Goal
- Patch documentation integrity issues from `review-claude/task-claude.md` before additional code cleanup.

Acceptance Criteria
- Range README indexes include phases 138, 139, and 140.
- New 141-150 compact plan/task/implementation summary files exist and are indexed.
- `.env.example` lists `EVENTREGISTRY_API_KEY`.
- Workflow docs state provider cache-version bump and live market-data test-isolation rules.

Completed Work
- Added missing phase 138-140 index entries, created the 141-150 compact bucket, added `EVENTREGISTRY_API_KEY`, and clarified cache-version and market-data test-isolation workflow rules.

## phase_142 - Market Data Test Isolation

Phase Goal
- Make fixed-market-data backend tests deterministic even when live data providers are installed locally.

Acceptance Criteria
- Unmocked backend tests do not call live `FinanceDataReader`.
- Tests that intentionally exercise FinanceDataReader behavior can still monkeypatch deterministic frames.
- Prior drift tests and the full backend suite pass.

Completed Work
- Added an autouse backend test fixture that blocks unmocked live `FinanceDataReader` calls while allowing explicit monkeypatched provider fakes.

## phase_143 - News Provider Robustness Cleanup

Phase Goal
- Address targeted backend news provider findings from the Claude review.

Acceptance Criteria
- EventRegistry REST calls use `https://www.eventregistry.org/api/v1/article`.
- Reddit partial subreddit failures can use a distinct partial provider-error run status.
- Reddit public search exits early on HTTP 429.
- The social query suffix is neutral and no longer bakes in time-bound political terms.
- News provider cache version is bumped after provider behavior changes.

Completed Work
- Updated EventRegistry host, Reddit host/rate-limit behavior, partial provider status, neutral social query suffix, and news provider cache version.

## phase_144 - News Evidence Timestamp Clamp

Phase Goal
- Prevent future-dated news digest fallback timestamps from entering prediction source documents.

Acceptance Criteria
- Missing article publication dates fall back to a timestamp no later than quote `as_of_at`.
- Future digest generated timestamps are clamped before conversion to source documents.
- Focused conversation coverage pins the clamp behavior.

Completed Work
- Added focused coverage and clamped only missing-article-date fallback timestamps, while keeping actual future article dates excludable.

## phase_145 - News Digest Card Privacy Cleanup

Phase Goal
- Improve news digest card rendering and remove third-party favicon URL leakage.

Acceptance Criteria
- Each news card renders the headline/title only once.
- News cards no longer call Google favicon URLs with article URLs.
- Focused frontend tests cover the title and favicon behavior.

Completed Work
- Made localized/news headlines the single link target and replaced remote favicon images with local domain glyphs.

## phase_146 - Settings Provider Type Cleanup

Phase Goal
- Remove stale app settings provider values that no longer drive backend LLM provider selection.

Acceptance Criteria
- Frontend `AppSettings` no longer exposes the dead `Provider = "openai" | "claude" | "gemini"` path.
- Backend settings responses no longer require or persist stale provider literals.
- API tests and settings tests reflect credential-profile provider selection as the real provider boundary.

Completed Work
- Removed stale settings provider fields from backend settings, local default state, frontend settings types, API mapping, defaults, and tests.

## phase_147 - Crawl URL Safety Boundary

Phase Goal
- Fix reviewed user-URL crawl security gaps.

Acceptance Criteria
- User-supplied crawl URLs are rejected when their host resolves to private, loopback, link-local, reserved, multicast, unspecified, or metadata IP ranges.
- Redirect targets are resolved and rejected with the same policy before response bodies are read.
- Unsupported response content types are rejected; missing `Content-Type` remains explicitly allowed.

Completed Work
- Added crawl URL safety regression tests and hardened direct crawl URL validation.

## phase_148 - Settings Credential Profile Actions

Phase Goal
- Fix reviewed Settings credential-management UX gaps.

Acceptance Criteria
- Saving a new LLM API key while another profile is active creates a new profile id instead of updating the active profile.
- Settings renders a delete action for each saved external news/search credential.
- Focused frontend tests cover both profile-action paths.

Completed Work
- Added focused SettingsModal tests and fixed LLM profile creation plus per-profile external delete rendering.

## phase_149 - Analysis External Credential Source Collection

Phase Goal
- Fix selected external credential wiring for stock-analysis source collection.

Acceptance Criteria
- Stock-analysis source collection uses selected Tavily, GNews, and SerpApi external credentials when present.
- Environment credential lookup remains the compatibility fallback when no selected key is provided.
- Regression coverage proves a saved selected Tavily key reaches the ingestion adapter without exposing the raw key in responses.

Completed Work
- Added focused conversation coverage and threaded selected external credentials into ingestion source collection.
